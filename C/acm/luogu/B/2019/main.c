#include <stdio.h>

int main() {
    int flag;
    scanf("%d", &flag);
    if (flag != 0)
    {   
        printf("%d\n", 1);
        return 0;
    }    
    printf("%d\n", 0);
    return 0;
}