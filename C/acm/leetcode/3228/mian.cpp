#include <string.h>
#include <iostream>

class Solution {
public:
    int maxOperations(string s) {
        
    }
};

int main(){
    char c[100];
    scanf("%s", c);
    printf("%d\n", maxOperations(c));
    return 0;
}